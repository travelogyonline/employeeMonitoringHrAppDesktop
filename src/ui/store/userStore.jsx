import { createContext } from "react";

export const UserStore = createContext(null);
export const DpStore =  createContext(null);
export const ThemeStore = createContext(null);
export const LanguageStore = createContext(null);